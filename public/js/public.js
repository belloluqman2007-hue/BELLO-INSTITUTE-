"use strict";

/*
 * Public-site feature module placeholder.
 *
 * Public pages were cleared from the frontend. The public API remains on the
 * server and is intentionally not called by this starter screen.
 */
window.Public = Object.freeze({});
